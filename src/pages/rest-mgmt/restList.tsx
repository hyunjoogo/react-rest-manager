import React, {useEffect} from 'react';

const RestList = () => {

  useEffect(() => {
    // TODO 권한 없으면 내쫓아
  }, [])

  useEffect(() => {
  }, [])

  const fetchUserRestList = async () => {

  }

  return (
    <div>

    </div>
  );
};

export default RestList;
